# HotStreak — Agent 前提

予測不能なドタバタ劇！ボードゲーム『ホットストリーク』を作るプロジェクト。

## 常時守ること

- ユーザーへの応答は日本語
- コーディング規約・詳細ルール・スキル一覧の正本は `.agents/AGENTS.md`
- 作業前に必要な節だけ `.agents/AGENTS.md` を読む（全部をルートに書かない）
- `docs/design/` は実装中リードオンリー。設計変更はユーザー明示時のみ
- `.cursor/` は触らない（rules / skills を増やさない）

## ルールの置き場

```
AGENTS.md                      # 本ファイル。約50行。前提とポインタのみ
.agents/AGENTS.md              # 生きた台帳（ルール本文 + スキル目次）
.agents/rules/<topic>.md       # 台帳から溢れた詳細
.agents/skills/<name>/SKILL.md # スキル本体
```

## 更新プロトコル（確認なし）

ユーザーがルールを直した・訂正したとき:

1. 確認せずに反映する
2. 言語・50行上限・正本の場所など本ファイルの不変事項だけ → 本ファイルを直す（50行以内）
3. それ以外の規約 → `.agents/AGENTS.md` を追記・修正
4. 1節が肥大化、または特定パスだけの話 → `.agents/rules/<topic>.md` へ移し、台帳にはリンクと1行要約だけ残す
5. 手順・ワークフロー → `.agents/skills/<name>/SKILL.md` を作り/直し、台帳のスキル目次だけ更新

## やらないこと

- 本ファイルにコーディング規約・API・UI 詳細を追記する
- 台帳を肥大化したまま放置する
- 空の規則ファイルを量産する
